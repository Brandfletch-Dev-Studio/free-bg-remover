services:
  - type: web
    name: free-bg-remover-api
    runtime: docker
    plan: free
    rootDir: server
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false
